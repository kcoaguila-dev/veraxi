# Veraxi Flutter Frontend Environment Variables
# Copy this to .env and add your secrets

# The DSN string from your sentry.io veraxi-frontend project
# Since we use --dart-define, the .env file might be read by a launch config or script
SENTRY_DSN=
